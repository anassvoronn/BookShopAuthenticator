Run "docker-compose down --volumes" to remove and clean up all old containers.
Run "docker-compose up --force-recreate" to create and start all the containers from scratch.

http://localhost:181/browser/ - to open the DB in the browser